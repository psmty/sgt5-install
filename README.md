# 📦 SGT5 Bootstrap Installer

This is the **development installer** for setting up the SGT5 system on your server or local environment.

PLEASE NOTE: This is a development installer and is not intended for production use.

---

## Installation

To begin, open your terminal and run the following command **in an empty folder**:

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/psmty/sgt5-install/main/install.sh)"
```